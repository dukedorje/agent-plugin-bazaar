# openspec

Two-layer process memory. Living truth is `specs/`. In-flight work is
`changes/`. Status is a banner in the file.

Read [`AGENTS.md`](AGENTS.md) and [`specs/living-specs/spec.md`](specs/living-specs/spec.md).
The `openspec` CLI is optional.
