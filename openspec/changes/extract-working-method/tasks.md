# Tasks

- [x] Child PENDING dirs exist: `add-working-loop`, `add-working-objects`, `add-working-split`, `update-founding-doc-pointer`
- [x] Parent delta names those change-ids and ADDs `working-method`
- [x] At scaffold, `/ready` listed all five as PENDING (parent is now ACTIVE BUILD; children still PENDING)

Out of scope (not a checkbox):
- Founding doc rewrite — `update-founding-doc-pointer`
- Implementing or folding children
