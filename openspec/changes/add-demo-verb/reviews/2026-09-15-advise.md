# advise — add-demo-verb (re-read after 2026-09-02 send-back)

> **ADVISE:** accept
> **READER:** fable-5.1-arch-review
> **SPAWN:** .spawns/add-demo-verb-1789531121-523773-f366dacb

## Blind take (Why, living specs, run.py EYES_RE only)

1. Pin: `demo` is a stage verb at act → demo → fold. Four roster blocks must gain it in one fold: verbs Purpose, verbs Shared references, packaging One skill tree, default-loop Default verbs. Inherited 2026-09-02 says last-writer-wins drops roster words; this owes a union box.
2. Pin: `EYES_RE` must not gain `demo`. Demo already has a hook: an owed EYES box whose text carries `Next: /demo <id>` is surfaced by `next_command`. Demo reading that box is fine; demo checking that box is forging eyes.
3. Pin: `run`'s decision tables (roll, empty, until-fold predicate) are untouched in this slice. `update-run-demo` owns `next: demo`. The default-loop "run may enter any loop stage" wave set must not silently gain `demo` here.
4. Refuse: any fourth store for demo residue. Same invent-no-tracker shape as map / steer. Residue goes on the bead, `tasks.md`, or chat.
5. Refuse: demo as a fold gate. Fold's legal predicate is ACTIVE BUILD, no open owed box, not PARKED. Adding "demo done" makes every `No new UI because` change unfoldable.
6. Pin: two non-working faces. Empty (nothing landed) prints `/status` and stops. `No new UI because` changes are also empty for demo, not an error.
7. Pin: three rings are vocabulary only. Internal SHALL be the only ring built. Mesoteric and exoteric SHALL NOT appear as invocable (default-loop "Catalogs list only skills that exist").
8. Pin: catalog consistency. `.agents/skills/demo` symlink, CLAUDE.md Skills Reference, README, plugin.json bump. Same as steer's landing.
9. Tradeoff: a verb versus just the `Next:` command on an EYES row. The verb wins on discoverability and packaging symmetry; the cost is a fifth roster word to keep in union across four blocks.
10. Concern outside this change: `run-wave` exists on disk but is missing from all three living rosters. The demo MODIFIED blocks should not further drop or silently repair it; the union box should name it.

Reader: Claude Fable 5.1 (other family; author is Grok). Rigor:
architecture. Permission: write on `reviews/` and `tasks.md` only.
No product path touched. Banner not flipped. Read against the
working tree; the amend is not yet committed (last commit on this
change is `2b0762c`, the send-back).

## Compare (take vs amended deltas, design, tasks)

Every line of the take is answered in the deltas or in `design.md`.

- **1 roster union** — all three MODIFIED blocks carry the current
  living roster plus `run-wave` plus `demo`. `add-run-wave-workflow`
  is still inflight; its packaging delta is a strict subset of this
  one, so either fold order is safe if the folder unions. The union
  instruction now lives in `tasks.md` and `design.md`, not in the
  packaging SHALL. Answered.
- **2 EYES_RE** — verbs delta: "SHALL NOT … add a `demo` token to
  the EYES / by-eye / human-verify vocabulary (`run.py` `EYES_RE`)"
  plus the "Demo invents no tracker" scenario. `run.py` has no
  `demo` today. Answered, and now pinned so an implementer cannot
  reach for it.
- **3 run tables untouched** — `run may enter any loop stage` and
  the roll / empty / fold tables are not in any delta; `design.md`
  names `update-run-demo` as owner. Answered.
- **4 no fourth store** — "Residue SHALL be at most a bead comment
  or a signed result with `permission: read`. It SHALL NOT write
  `demo.md`, mint an owed box …" with a rejection scenario in the
  same shape as steer and map. Answered.
- **5 not a fold gate** — SHALL body, a scenario, and a `design.md`
  section. The single predicate stays `update-run-ooda`'s. Answered.
- **6 empty faces** — Empty scenario present (no inflight, no
  archive → print `/status`, stop, write nothing). Archive
  resolution scenario present. Answered.
- **7 rings vocabulary only** — unchanged from the first read;
  `design.md` "Do not grow three skills." Answered.
- **8 catalogs** — `tasks.md` boxes for skill, symlink, `shared.md`
  row, catalogs, `check-skills.py`. Answered.
- **9 verb vs face** — `design.md` "a stage, not observe … and not a
  mailbox face (EYES)". Same conclusion as the take.
- **10 run-wave drift** — not this change's to repair, and the
  deltas do not silently repair it; they add `run-wave` alongside
  the sibling change that owns it. Fine.

The three owed boxes from 2026-09-02 are each satisfied by the text
they name. Checked boxes are honest.

## Steelman

Against my own take: one could argue the roster union is still
fragile because it depends on a human folder reading `tasks.md`.
True, but the alternative the first read rejected (process text in
a living SHALL) is worse, and LEARNINGS already carries the rule.
The delta is the right place to stop.

## Notes (body, not verdict; none owed)

- **Bare `/demo` resolution.** The SHALL body now names the journey
  source and the change-dir lookup, but no longer says how bare
  `/demo` (no id) picks a change. The Empty scenario's "(and no
  current intend DAG landing)" is the only trace of the old
  "current intend DAG" source. One phrase in the body ("with no id,
  the id is the current intend DAG's last landed act") would close
  it. Not a shape question; the skill body can carry it.
- **Halt clause.** Still not in the delta (carried over from the
  first read). "demo SHALL stop after presenting the journey; it
  SHALL NOT continue into fold or act." `update-run-demo` will want
  to cite it. Not owed here.
- **Self-referential fold box.** `tasks.md` "Fold living specs when
  this change folds …" is an open owed box that only fold can
  check, so the change is never fold-legal until someone checks it
  by hand. Move the union reminder into the task's prose or drop
  the box. Not owed; a fold-time nuisance, not an architecture
  defect.
- **verbs Purpose paragraph.** The roster sentence under Purpose is
  not a requirement block, so no delta touches it. The folder adds
  `demo` (and `run-wave`) there by hand.
- **Persist.** The amend is uncommitted. The conductor should
  persist `design.md`, `tasks.md`, and the three deltas before
  dispatching act, or the worker packet reads the send-back state.
