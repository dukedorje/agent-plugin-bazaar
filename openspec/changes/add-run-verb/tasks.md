# Tasks

- [x] `plugins/intention/skills/run/SKILL.md` + `.agents/skills/run` symlink
- [x] `shared.md` verb table row: `run` produces a campaign result; must not invent a second packet schema or slash a foreign worker
- [x] Catalogs (plugin.json description, README / harness install line, default-loop presentation) list `run`
- [x] Packet-only slash denylist includes `/run` (spawn + verbs SHALL)
- [x] Focused test: empty ready-set → run card, no worker launched
