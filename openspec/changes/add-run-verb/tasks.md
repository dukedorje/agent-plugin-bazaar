# Tasks

- [ ] `plugins/intention/skills/run/SKILL.md` + `.agents/skills/run` symlink
- [ ] `shared.md` verb table row: `run` produces a campaign result; must not invent a second packet schema or slash a foreign worker
- [ ] Catalogs (plugin.json description, README / harness install line, default-loop presentation) list `run`
- [ ] Packet-only slash denylist includes `/run` (spawn + verbs SHALL)
- [ ] Focused test: empty ready-set → run card, no worker launched
