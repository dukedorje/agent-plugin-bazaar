# advise update-run-gates

> **ADVISE:** send-back

Reader: Grok 4.6 (`ladder.py assign --shape architecture-review` →
`grok-arch-review`). Permission: read. I authored the scaffold
(`4bf3661`). This verdict does not promote (ADR-005;
`openspec/specs/verbs/spec.md:86-87`).

Bare `/advise` — sibling `update-run-ooda` is also send-back.

## Verdict

Send-back. The default walk is the right fence. It currently
restates settlements ooda still owes, so act on gates would
invent the same detector twice.

## Steelman against

Do not split gates from ooda. One MODIFIED campaign requirement
can name both the `next` set and the default walk. A second
change is ceremony. ADDED “run gate defaults” will duplicate
`--until empty` / `--until fold` / `--autonomous` already in
the campaign block (`openspec/specs/verbs/spec.md:141-150` and
the ooda paste).

That steelman loses on purpose. Dual-MODIFY of the same
requirement is how fold drops clauses. ADDED keeps the table
addressable after ooda folds. The cost is overlap. Hold the
fence: ooda owns *which* `next` values exist; gates owns *when*
the default walk enters them. Today gates re-owns `next: fold`
(`openspec/changes/update-run-gates/specs/verbs/spec.md:10-11`,
`:31-34`). That is the failure mode.

## One real tradeoff

**Empty does not fold** is the product. Making fold the default
after every act is how a campaign becomes archive-theater.
Keeping fold behind `--until fold` matches living law
(`openspec/specs/verbs/spec.md:132-134`). The cost is Duke still
types `--until fold` (or `/fold`) when the walk is otherwise
empty. That is the right cost.

## Findings

1. **Blocking — same unnamed detector.** “Do not intend unless
   the scope is a goal” (`proposal.md:23-25`;
   `specs/verbs/spec.md:7-8`, `:24-29`) has no detector. It
   must use whatever `update-run-ooda` names. Ooda is send-back
   on that name. Do not let gates invent a second one.

2. **Blocking — `next: fold` re-owned.** Ooda already emits
   fold when `--until fold` and fold is legal. Gates’ scenario
   “Until fold emits fold when legal”
   (`specs/verbs/spec.md:31-34`) is the same acceptance
   surface. Cite ooda’s legal-fold predicate. Keep the empty
   *does not* fold scenario — that is this change.

3. **Nit — autonomous scenario is already living.** “Autonomous
   does not flip PENDING” (`specs/verbs/spec.md:36-41`) restates
   `openspec/specs/verbs/spec.md:146-150` and the living
   “Autonomous does not forge eyes” scenario. Fine as a gate
   table row; do not grow a third SHALL.

## What is solid

- Default empty = change → advise → act. Stop at PENDING, ASK,
  and fold. That is the anti-theater fence.
- `--autonomous` uses the same walk and does not flip gates.
- No new verbs. No `--until intend` token.
- Journey is honest. Banner stays ACTIVE BUILD.

## Implementer gaps

Amend after ooda’s detector and legal-fold predicate exist.
Act on gates is not dispatchable until those boxes close and
ooda has landed a usable artifact (`bazaar-8uv.2` depends on
`bazaar-8uv.1`). Do not fold. Do not flip PENDING.

A later accept must not be this author as the sole reader.
