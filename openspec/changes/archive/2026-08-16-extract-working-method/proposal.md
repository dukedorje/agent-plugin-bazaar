# extract-working-method

> **ACTIVE BUILD** → folded and archived 2026-08-16.

## Why

`docs/from-intention-to-running.md` is still two things at once: the
method of working, and the first use of the method on itself. The
method is already partly folded (`verbs`, `living-specs`,
`agent-surface`, `default-loop`). What remains in the novel is the
user-facing algebra — loop, objects, split rules — with no landing
change-ids. This extract names those landings. It does not cut the
file yet.

## What

Name four PENDING intentions. Activate them one at a time.

| Change-id | Slice of the founding doc |
|---|---|
| `add-working-loop` | The claim, OODA loop, rigor dial, load class |
| `add-working-objects` | The seven objects + the brief |
| `add-working-split` | One acceptance, real edges, ready-set, named change-ids |
| `update-founding-doc-pointer` | After the three fold: the novel becomes a pointer |

Capability `working-method` is ADDED by this parent (materialized when
this change folds). Children add requirements onto it.

## Impact

- Capabilities: ADDED `working-method` (at fold of this parent)
- ADRs: none
- Does not edit `docs/from-intention-to-running.md` until
  `update-founding-doc-pointer`

## User journey & surfaces

No new UI because the surfaces are `/ready`, `/change`, and the
founding doc. Working: `/ready` lists these as NEEDS ACTIVATION.
Empty: founding doc still reads as the source. Failed: someone treats
the novel as a second requirements store. Off: leave PENDING; the
method stays in the novel.

## Out of scope

- Implementing or folding any child (activate each first)
- Rewriting `verbs` / `agent-surface` (already folded)
- Taskmaster site work (`bazaar-lgr`)
- Unparking sprint-plan or F1-path-b
