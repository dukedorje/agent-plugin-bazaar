# park-sprint-plan

> **ACTIVE BUILD** → folded and archived 2026-08-15 (D1).

## Why

Dogfood. The founding DAG said park or shrink `/sprint-plan` with a
revive condition. Default is the verb loop.

## What

Park the skill (banner + no auto-invoke). Point README, marketplace,
CLAUDE.md, and CONVENTIONS at `intend`. Do not delete phases.

## User journey & surfaces

No new UI because the surfaces are README install, skill discovery, and
`/sprint-plan` if someone types it anyway. Working: they install
`intention` and run `intend`. Empty: they still have morphist-tools for
PRD/vision/research. Failed: a model auto-starts Phase 0. Off: they
explicitly revive the factory.

## Out of scope

- Deleting sprint-plan phases
- Rewriting every morphist skill that mentions `/sprint-plan`
- P1 hosts
