# Tasks

- [x] `claude -p` adapter: model/effort from interface, slash-off
- [x] Stub-bin test: argv contains `-p`, model, prompt file contents
- [x] `max_inflight` + `ACT_MAX_INFLIGHT` + `--max-inflight`
- [x] `take` / `release` lease; second take fails; overlap still defers
- [x] Wire act / act-io; living-spec delta
