# add-harness-touch-ins

> **ACTIVE BUILD** → folded and archived 2026-08-15 (H1).

## Why

S1–S4 exist. Five hosts need a written reduce: who loads the tree, who
is packet-only, what the Vercel `skills` CLI can and cannot do.

## What

Fan-out notes collapsed into `plugins/intention/references/harness.md`.
Living spec `harness` points at it. `check-skills.py` lists via `skills
add --list` when the binary is present.

## User journey & surfaces

No new UI because the surfaces are skill discovery in each harness and
the matrix file. Working: Grok in this clone sees the five verbs; another
repo runs `skills add ./plugins/intention --list` and sees the same five.
Empty: a host with no scan of `.agents/skills/` and no install. Failed:
`skills add` of the bazaar *root* offering sprint-plan. Off: do not
install; in-repo Grok still works.

## Out of scope

- Teaching vercel-labs/skills about Grok/Hermes
- Running `skills add` inside this repo
- G1 hygiene
- D1 sprint-plan park
