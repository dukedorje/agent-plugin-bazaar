# advise add-paste-objects (after amend)

> **ADVISE:** accept-with-nits

Reader: Grok 4.6 (`ladder.py assign --shape architecture-review`).
Permission: read. Duke said `continue` after the same-family warning;
the human is the second reader of this accept. I authored the amend
(`2d7908d`). Nits are not owed boxes.

## Verdict

Accept-with-nits. The send-back is named. Mixed paste cites. A lone
task list does not invent a dossier. Act may write ADR-008.

## Steelman against

Keep independent category piles — grammar is simpler, and ADR-007
already requires a cite when someone *claims* emerge. A paste face
should not restated emerge.

That steelman loses. Duke’s document is one paste with both piles.
`design.md:3-4` still says act does not reopen. Without the mixed
rule on *this* face, grammar would drop the cite and still call the
items emerged.

## One real tradeoff

**Thicker face vs thinner grammar.** The mixed-paste rule makes
this change a little larger than “map each section.” The cost of
being thin is silent provenance loss. Thickness here is cheaper
than a later store.

## Findings

1. **Closed — mixed paste cites.**
   `specs/dossier/spec.md:36-53` (mixed requirement + scenario).
2. **Closed — lone task invents nothing.**
   `specs/dossier/spec.md:27-34` and `62-66`.
3. **Closed — intend-dag is intentions plus work nodes.**
   `specs/dossier/spec.md:7-8`, `19-25`.
4. **Closed — SHALL gone from design.md.**
5. **Nit — lone intend-dag has a SHALL and no scenario.**
   `specs/dossier/spec.md:44-45` says intentions minted with no
   gathering are not recorded as emerged. There is no matching
   `#### Scenario`. Grammar can still implement the SHALL. Act
   should not invent a new mapping; a scenario can wait for fold
   or `add-paste-grammar`.

## What is solid

- Face of existing objects. Kernel authority. Leftover `task`
  table rejected. Cards are not `/`. Bytes wait on `bazaar-ja7`.
  TUI is a client. Depends on living ADR-007, not a second kinds
  ADR. Journey failed-path now includes dropped cite.

## Implementer gaps

Act copies `design.md` into ADR-008. It does not write a parser,
a host table, a TUI, or cards. Fold after that box is checked.
