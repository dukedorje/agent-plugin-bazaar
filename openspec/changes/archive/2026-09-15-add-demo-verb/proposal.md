# add-demo-verb

> **ACTIVE BUILD**

**Rigor:** architecture

Activated 2026-09-02 (chat: activate demo verb). Steer residue:
one verb, three rings; slice one is internal.

## Why

`act` implements. `fold` archives. Nothing in the loop is “the
human tries it.” EYES is a look-owed checkbox on `/status`, not a
stage. Deploy is in `do_not`. `/run-wave` EYES was a demo with no
verb.

## What

- Add verb **`demo`**: after `act`, before `fold`, the iteration is
  displayed and experienced.
- One verb, three rings. Slice one is **internal** (human tries the
  feature). Mesoteric (staging) and exoteric (production) are named
  later (`bazaar-spi.2`), not built here.
- Does not flip EYES boxes. Does not deploy. Does not fold.
- Capabilities: ADDED on `verbs`. MODIFIED `packaging`,
  `default-loop`, shared-references list on `verbs`.

## Impact

- Capabilities: MODIFIED `verbs`, `default-loop`, `packaging`
- ADRs: will amend `ARCHITECTURE.md` (verb list) when this folds

## User journey & surfaces

Duke, from a Grok tab, after a landed `act`.

1. Says `/demo` (or `/demo add-run-wave-workflow`).
2. **Working** — a named journey so he can try the feature (CLI,
   TUI, or the Next command on the change). Halt is loud, like
   YOUR EYES.
3. **Empty** — nothing landed to try: print `/status` and stop.
4. **Off** — skill missing; EYES boxes remain the poor substitute.

`No new UI because` `/demo` and the host already used for `/run`
halts.

## Out of scope

- Staging deploy (mesoteric ring — `bazaar-spi.2`)
- Production deploy (exoteric ring — `bazaar-spi.2`)
- Campaign walk `next: demo` (`update-run-demo`, `bazaar-spi.3`)
- Checking EYES boxes
- Unparking P1 hosts for a deploy target
- Fileset organizer / worktree land
