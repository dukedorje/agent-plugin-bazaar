# steer add-demo-verb

**When.** 2026-09-02
**Depth.** lean (chat activate skipped the menu pass)

## Decided

- One verb `demo`, not three. Rings: internal (human tries the
  feature) · mesoteric (staging) · exoteric (production).
- Slice one is **internal** only. Staging and production are named,
  not built (`bazaar-spi.2`).
- After `act`, before `fold`. The iteration is displayed and
  experienced. EYES is a mailbox face, not this stage.
- Campaign walk is a later node (`update-run-demo`). This change
  ships the skill and the loop sentence.

## Skipped

- Formal menu pass (user said activate)

## Feeds change

`/demo` on a landed act: human can try the feature. No deploy.
No EYES-box flip. No fold.
