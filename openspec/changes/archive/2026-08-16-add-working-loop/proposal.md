# add-working-loop

> **ACTIVE BUILD** → folded and archived 2026-08-16.

Depends on: `extract-working-method` (names this id).

## Why

The loop, rigor dial, and load class are the method's runtime. They
live in `docs/from-intention-to-running.md` §§ The claim / The loop.
They are not living SHALLs.

## What

Fold those sections onto `working-method`: observe → orient → decide
→ act; rigor is a dial; ceremony escalates only; load class sets
posture.

## Impact

- Capabilities: MODIFIED `working-method`
- ADRs: none

## User journey & surfaces

No new UI because the surfaces are `intend` (orients) and the founding
doc. Working: an agent orients with load × blast × lifecycle before
splitting. Empty: they treat every task as a sprint. Failed: a typo
earns a requirements document. Off: leave PENDING.

## Out of scope

- Cutting the founding doc (`update-founding-doc-pointer`)
- Ladder assignment (ADR-005 / `ladder.json`)
