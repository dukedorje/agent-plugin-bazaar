# Tasks

- [x] `run.py` accepts a positional scope and emits `change` / `advise` / `act`
- [x] `stop: no-ready` only when `ready.py` is missing
- [x] `test-run.py` covers scope-missing-dir, advise-before-act, until-advise-does-not-act
- [x] `run/SKILL.md` matches the card: follow `next`, do not halt on a named new change-id
