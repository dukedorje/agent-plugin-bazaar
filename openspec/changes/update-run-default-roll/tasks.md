# Tasks

- [x] `run.py`: default `--until roll`
- [x] Skill + README: empty is the cautious walk, not the default
- [x] `--autonomous` alone uses roll
- [x] verbs delta (run gate defaults)
- [x] Tests: default fold-legal is fold; empty still refuses fold when named
