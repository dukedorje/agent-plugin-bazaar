# Tasks

Work **this** change owes. The host page is `bazaar-lgr.5`.

- [x] Script: project `bd` (or `bd export`) into the settled snapshot shape
- [x] `generated_at` required; no `ready` field; closed → `landed`; omit deferred/parked
- [x] Drop `needs` whose id is not in `nodes`
- [x] Focused test against a fixture inventory
- [x] Document the output path the app vendors at deploy
