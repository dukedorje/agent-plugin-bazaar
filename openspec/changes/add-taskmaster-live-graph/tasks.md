# Tasks

Work **this** change owes. The host page is `bazaar-lgr.5`.

- [ ] Script: project `bd` (or `bd export`) into the settled snapshot shape
- [ ] `generated_at` required; no `ready` field; closed → `landed`; omit deferred/parked
- [ ] Drop `needs` whose id is not in `nodes`
- [ ] Focused test against a fixture inventory
- [ ] Document the output path the app vendors at deploy
