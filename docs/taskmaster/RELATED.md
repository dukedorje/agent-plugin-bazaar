# Related intentions (a weave)

Cross-repo work that shares Taskmaster’s objects but does **not** live
in this marketplace. This is a weave, not a parent epic: complementary
landings, one signed story when we talk about “the project.”

If a row dies, date-stamp it. If a path is unknown, leave it blank and
keep the bead.

| Intention | Landing | What it owes / is owed | Tracker |
|---|---|---|---|
| Taskmaster SaaS | Mjolnir guest `/taskmaster-web`; notes in this folder | Public ready-set: nodes, assignments, evidence | this file + `add-taskmaster-host` |
| Intention kernel | `~/work/Projects/AI/agent-plugin-bazaar` | Packets, verbs, living specs. App consumes, does not fork | living `openspec/specs/` |
| MetaCoding | `~/work/WorldTree/MetaCoding` | Code graph + **category theory on ASTs** (`ctkr`). Ingestion source | `bazaar-ja7` |
| Fong’s Meta Env (our copy) | *(path unknown — fill in)* | Finish the copy. Peer landing, not a Taskmaster feature | `bazaar-zmq` |
| Mjolnir | `~/work/IdentiKey/mjolnir` | Boxes, PTY, later snapshot deploys. Action surface | P1 still parked as a *product*; we *use* the fabric |
| IdentiKey login | IdentiKey team | Auth UI. Hop when it blocks; do not fake an IdP | note in ARCHITECTURE |
| Dreamballs / G Brain | see `bazaar-ja7` | Ingestion candidates with MetaCoding | `bazaar-ja7` |

Do not invent a fourth knowledge store before sitting on `bazaar-ja7`.
Do not let the SaaS absorb Meta Env — link it here, work it there.
